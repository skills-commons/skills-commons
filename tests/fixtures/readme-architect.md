Repo: a Python CLI called fetchr. Layout: fetchr/__main__.py, fetchr/core.py,
fetchr/plugins/, tests/, pyproject.toml, and a docs/ folder with two stale files.
The existing README is one line: "fetchr - fetch things". The issue tracker keeps
getting "how do I install this" and "does it work on Windows".
